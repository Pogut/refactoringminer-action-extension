{
  "name": "refactoringminer-action-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that overlays refactoringminer-action's detected refactorings onto GitHub diffs.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "HEADED=1 playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
